<div class="col-lg-9">

    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-secondary">Mis datos</h6>
        </div>
        <div class="card-body">
            {{#if isProminent}}
            <h2>¡Hola <span class="font-weight-bold text-warning mb-5">{{user.name}}</span>!</h2>
            {{else}}
            <h2>¡Hola <span class="font-weight-bold mb-5">{{user.name}}</span>!</h2>
            {{/if}}
            <p class="ml-3">Aquí tendrás el control de tu perfil, sientete libre de cambiarlo y personalizarlo.</p>
            <div class="my-2"></div>
                                    <div class="row">
                                        <div class="col-lg-4 d-flex justify-content-center">
                                            <div class="card shadow mt-2">
                                                <div class="card-body bg-light">
                                                    <div class="d-none d-sm-inline-block form-inline mr-auto my-2 my-md-0 mw-100">
                                                        <p class="small mb-1">Nombre</p>
                                                        <div class="input-group">
                                                            <input type="text" class="form-control bg-light small" value="{{user.name}}" readonly>
                                                        </div>
                                                        <p class="small mb-1">Apellido</p>
                                                        <div class="input-group">
                                                            <input type="text" class="form-control bg-light small" value="{{user.surname}}" readonly>
                                                        </div>
                                                        <p class="small mb-1 mt-2">Nacimiento</p>
                                                        <div class="input-group">
                                                            <input type="text" class="form-control bg-light small" value="{{user.nacimiento}}" readonly>
                                                        </div>
                                                        <p class="small mb-1 mt-2">Dirección</p>
                                                        <div class="input-group">
                                                            <input type="text" class="form-control bg-light small" value="{{user.direccion}}" readonly>
                                                        </div>
                                                        
                                                        <div class="input-group mt-3">
                                                            <a class="btn btn-secondary" href="/profile/edit">
                                                                Editar información <i class="fas fa-pencil-alt fa-sm"></i>
                                                            </a>
                                                        </div>
                                                    </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>