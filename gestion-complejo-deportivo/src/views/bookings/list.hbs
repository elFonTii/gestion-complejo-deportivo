<a href="bookings/create" class="btn btn-success p-2 mr-4 float-right">Nueva reserva</a>
<div class="container p-4 m-2">
  <div class="row">
    <div class="col-md-3">
      {{#each bookings}}
          <div class="card text-center bg-dark card-outline-secondary">
            <div class="card-body">
                    <h3 class="card-title text-light text-uppercase font-weight-bold">
                        {{tipo_cancha}}
                    </h3>
                <p class="text-light"></p>
                <p class="text-light">Precio: UYU {{price}}</p>
                <p class="text-light">{{timeago create_at}}</p>
                <a href="/bookings/delete/{{id_booking}}" class="btn btn-danger">Cancelar</a>
                <a href="" class="btn btn-secondary">Visualizar</a>
            </div>
      {{else}}
        <div class="col-md-12 mx-auto"> 
          <div class="card card-body text-center">
            <p>Ooh... parece que no hay reservas aún</p>
            <a href="/bookings/create" class="btn btn-success">Crear una</a>
          </div>
        </div>
      {{/each}}
      </div>
  </div>
</div>